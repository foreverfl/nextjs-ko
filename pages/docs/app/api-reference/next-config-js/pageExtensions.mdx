---
title: pageExtensions
description: Pages 라우터에서 Next.js가 페이지를 해결할 때 사용하는 기본 페이지 확장자를 확장합니다.
---

# pageExtensions

{/* 이 문서의 내용은 app 라우터와 pages 라우터 간에 공유됩니다. `<PagesOnly>Content</PagesOnly>` 컴포넌트를 사용하여 Pages 라우터에만 특정한 내용을 추가할 수 있습니다. 공유된 내용은 컴포넌트로 감싸지 않아야 합니다. */}

기본적으로 Next.js는 다음 확장자를 가진 파일을 허용합니다: `.tsx`, `.ts`, `.jsx`, `.js`. 이는 마크다운(`.md`, `.mdx`)과 같은 다른 확장자를 허용하도록 수정할 수 있습니다.

```js filename="next.config.js"
const withMDX = require("@next/mdx")();

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ["js", "jsx", "ts", "tsx", "md", "mdx"],
};

module.exports = withMDX(nextConfig);
```
