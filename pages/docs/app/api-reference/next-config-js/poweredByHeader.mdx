---
title: poweredByHeader
description: Next.js는 기본적으로 `x-powered-by` 헤더를 추가합니다. 여기서 이를 비활성화하는 방법을 알아보세요.
---

# poweredByHeader

{/* 이 문서의 내용은 app 라우터와 pages 라우터 간에 공유됩니다. `<PagesOnly>Content</PagesOnly>` 컴포넌트를 사용하여 Pages 라우터에만 특정한 내용을 추가할 수 있습니다. 공유된 내용은 컴포넌트로 감싸지 않아야 합니다. */}

기본적으로 Next.js는 `x-powered-by` 헤더를 추가합니다. 이를 비활성화하려면 `next.config.js`를 열고 `poweredByHeader` 설정을 비활성화하세요:

```js filename="next.config.js"
module.exports = {
  poweredByHeader: false,
};
```
